{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch tests",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${cmake.testWorkingDirectory}",
            "program": "${cmake.testProgram}",
            "args": [
                "${cmake.testArgs}"
            ],
        }
    ]
}
